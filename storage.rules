rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read for all clients (menu images, uploads)
      allow read: if true;
      // Allow write for authenticated users
      allow write: if request.auth != null;
    }
  }
}
